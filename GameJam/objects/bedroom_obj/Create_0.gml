// Zoom variables
zoom_level = 1.0;
zoom_speed = 0.0005; // Very slow zoom for cinematic effect

// Ensure views are active
view_enabled = true;
view_visible[0] = true;

// Timer to create the popup at 3 seconds
alarm_set(0, room_speed * 3);
